language: node_js
node_js:
    - "4.1"
    - "5.0"
    - "6.0"

before_install:
    - sudo apt-get -qq install -y wine

before_script:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start
    - sleep 3
